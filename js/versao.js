/**
 * VERSAO.JS
 * ---------------------------------------------------------------------------
 * ARQUIVO GERADO NO DEPLOY — não edite à mão esperando que o valor sobreviva.
 *
 * O site não tem build step, então este arquivo é commitado com o valor
 * "dev" (é o que aparece ao servir os arquivos localmente). No Cloudflare
 * Pages, o comando de build sobrescreve o arquivo com o hash real do
 * commit — ver "Carimbo de versão" no README.
 *
 * Formato: o hash completo (40 caracteres) ou "dev". Quem decide o que
 * mostrar é js/rodape.js.
 * ---------------------------------------------------------------------------
 */
window.ACTA_VERSAO = "dev";
